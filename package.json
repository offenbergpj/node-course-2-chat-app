{
  "name": "node-chat-app",
  "version": "0.0.0",
  "description": "node-chat-app",
  "main": "app.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha --timeout=4000 server/**/*.test.js",
    "test-watch": "nodemon --exec \"npm test\""
  },
  "engines": {
    "node": "10.1.0"
  },
  "author": {
    "name": "offenberg"
  },
  "dependencies": {
    "express": "^4.16.3",
    "moment": "^2.22.2",
    "socket.io": "^2.1.1"
  },
  "devDependencies": {
    "expect": "^23.2.0",
    "mocha": "^5.2.0",
    "nodemon": "^1.17.5"
  }
}
